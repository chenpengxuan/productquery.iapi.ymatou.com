description = "this is a test jar"

jar {
	baseName = "productquery-test"
}

dependencies {
	compile(project(":web"))
	compile "org.springframework.boot:spring-boot-starter-test:${springBootVersion}"
}


